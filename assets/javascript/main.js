(function(exports) {
	/*jshint mootools:true,smarttabs:true*/
	'use strict';
	
	/**
	 * Main function
	 * Run when the DOM is ready
	 */
	function main() {
		
	}
	
	// Run main when the DOM is ready
	document.addEvent('domready', main);
}(this));